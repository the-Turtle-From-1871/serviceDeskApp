// Stub for `server-only`/`client-only` in the Vitest (Node) environment.
// Those packages are build-time guards that throw when imported outside the
// Next.js RSC/client bundler; tests import the underlying modules directly.
export {};
